## Load rematerialization oprimization experiment

Here you can find implementation in Kotlin/Native. C version can be found [here](https://godbolt.org/).

To run the test, just execute `runDebugExecutableLinuxX64` Gradle configuration, or a similar one according to your machine's architecture.
